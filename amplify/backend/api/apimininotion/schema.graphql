type User @model @auth(rules: [{allow: public}]) {
  id: ID!
  userName: String
  NotesToOneUser: [Note] @connection(keyName: "byUser", fields: ["id"])
}

type Note @model @auth(rules: [{allow: public}]) @key(name: "byUser", fields: ["userID"]) {
  id: ID!
  content: String
  userID: ID!
}
